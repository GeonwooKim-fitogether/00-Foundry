# v0.3.0 Candidate — Elevation Summary (Director-facing)

**Status**: v0.3.0 candidate · Drafted 2026-05-18 · Director Acceptance pending
**Source cycle**: `12.subscription-payment-saas-platform` Phase 1~5 (WP-002 ~ WP-005)
**Predecessor**: v0.2.1 frozen (2026-05-12)

본 문서 = Director 가 v0.3.0 격상 의사결정에 필요한 *single entry point*. 한 화면 안에 5 spot.

---

## Spot 1 — 격상 대상 파일 list (작은 list)

### 신규 파일 (2)
- `4_Task_Level_Execution_Templates/Next_Cycle_Selection_Rule.md` *(v1.0 candidate, ~70 lines)* — D-025 standard.
- `Bootstrap/Foundry_FIL_Ledger.md` *(v0.3.0 candidate, ~150 lines)* — adopted FIL 박제 (FIL-008 + FIL-NEW + FIL-WP005-pattern).
- `Bootstrap/v0.3.0_Candidate_Elevation_Summary.md` *(본 문서)*.

### 갱신 파일 (4)
- `4_Task_Level_Execution_Templates/Director_Card_Template.md` — v1.0 → v1.1 candidate. 헤더 + 추가 조건 A/B/C/D 섹션 + self-audit 체크리스트 9→12.
- `1_Universal_Operating_Principles/Non_Blocking_Execution_정책.md` — §8 amendment append (D-025 cross-ref) + 변경이력 1행.
- `MANIFEST.md` — header 갱신 (v0.2.1 frozen → v0.3.0 candidate) + L4 신규 2 + Bootstrap 신규 2 + L1 Non_Blocking 표기.
- `CHANGELOG.md` — v0.3.0 candidate entry (~70 lines) append (v0.2.1 위에).
- `README.md` — Status 라인 v0.3.0 candidate 표기 추가.

### 변경 0 (의도적)
- runner / hook / skill / NPI_Brief 템플릿 / Naming Map / Document Terminology Map / Phase + WP-NNN naming / factory.yaml.
- source project (`12.subscription-payment-saas-platform`) 어떤 파일도 변경 0.

---

## Spot 2 — 신규 표준 (2 + 1 amendment)

### (A) Director Card Template v1.1 — 추가 조건 A/B/C/D
4약속 (읽기/판단/디버깅/메커니즘 부담 0) 위에 positive-axis 4 조건:

| 조건 | 의미 |
|---|---|
| **A** 맥락 무지 가정 | 이전 스크린샷 / 카드 / chat / 코드 변경 참조 0. 카드 안에서 모든 사실 재진술. |
| **B** Implementer step 분리 | git / npm / build / test / lint 류 = Implementer 가 카드 발급 *전* 에 끝냄. Director 위임 = Director-only 행동 only. |
| **C** GPT orchestrator 통과 | Director → GPT(paste 중계) → Director 1줄 action 으로 환원 가능. GPT 재해석 / 추가 질문 0. |
| **D** 환경 가정 금지 | Vim/Emacs 키 시퀀스 / 창 식별 / bash 명령 (`cat`/`grep`/`sed`) / PS 5.1 비호환 chain (`&&`, `||`, `2>&1`) 0. 단일 PS 5.1 호환 블록. |

source: 12.subscription `meta/decisions.md` D-024 (예정 박제).

### (B) Next Cycle Selection Rule (D-025) — D-017 폐기
- **non-HCP next cycle 선택 = Implementer 자율 default**. Director Card 발급 0.
- HCP gate / Final CP / scope drift 만 Director Card.
- 우선순위 휴리스틱: `deferred 제외` > `Foundry backport` > `planning draft` > `follow-up` > `HCP-only 카드` (마지막).
- D-017 ("Implementer 가 next-cycle 후보 자발 제시 금지") 폐기 — reduced-copy Stage 2 와 정반대 방향이었기에.

### (C) Non_Blocking_Execution_정책 §8 amendment (cross-ref)
- (B) 의 표준을 L1 invariant 와 정합시키는 짧은 §8 append (~10 lines). 본문 = L4 표준 참조.

---

## Spot 3 — 새 FIL Ledger entries (3)

`Bootstrap/Foundry_FIL_Ledger.md` 신설. 본 v0.3.0 채택 3 entry:

| FIL | 제목 | 핵심 |
|---|---|---|
| **FIL-008** | Windows `desktop.ini` `.git` 트리 침투 | 표준 cleanup 스크립트 1줄 박제. 재현 N=7+ (Foundry main repo 본 cycle 중에도 재발 → 동일 스크립트로 해소). |
| **FIL-NEW (FIL-014 후보)** | agent worktree `.env.local` 부재 | Implementer 가 main worktree 에서 *읽기-전용 복사*. Director Card 발급 0. secret 노출 anti-pattern 박제. |
| **FIL-WP005-pattern** | frozen file 1-block addition 패턴 | read-mostly cycle (Invoice / Statement / Audit 등) 의 frozen body 0 rewrite + 신규 1 block append + Director-gated. WP-005 = first instance (D-023). |

미채택 (보류 / 폐기) FIL 은 ledger 외부 (source project `meta/foundry-backport-candidates.md` 의 *보류* 분류 유지). 다음 Meta Sprint 재평가.

---

## Spot 4 — B-* revalidation 결과

본 v0.3.0 candidate = *정책/문서 add only*, runner/hook/skill 코드 변경 0. 따라서 회귀 0 보장.

| Self-test | 명령 | Expected | 시점 |
|---|---|---|---|
| **B-002** | `python run.py --brief 3_Domain_Project_Playbooks/ai-npi-platform/B-002_NPI_Brief.md` | total=6 pass=6 fail=0 exit=0 | Director Acceptance 직전 1회. |
| **B-003** | `python run.py --brief 3_Domain_Project_Playbooks/ai-npi-platform/B-003_NPI_Brief.md` | total=9 pass=9 fail=0 exit=0 | Director Acceptance 직전 1회. |
| **B-004** | (미생성) | n/a | v0.3.0 frozen 격상 시점에 *신규 AC 박제 여부* Director 결정. 본 draft = 생성 0. |

> 본 draft 단계에서는 self-test 미실행. Director Acceptance 1-line 직후 Implementer 가 위 2 명령 실행 → 결과 박제 → frozen 격상 commit.

---

## Spot 5 — Director Acceptance prompt (1줄)

**"진행"** (단순 1-line, reduced-copy default)

또는, 부분 수정 요청 시:

- "Spot N 의 X 만 빼고 진행" — 해당 항목 draft 에서 제거 후 진행.
- "전체 보류" — 본 candidate 를 Backlog 으로 이동, 다음 cycle 재평가.

Acceptance 후 Implementer 가 자율 처리:
1. B-002 + B-003 self-test 실행 + 결과 박제.
2. `factory.yaml` version bump `0.2.1` → `0.3.0` + `status: frozen` + `frozen_on: 2026-05-XX`.
3. CHANGELOG `[v0.3.0]` header 의 *candidate* → *frozen* 갱신.
4. README Status 라인 갱신.
5. commit + push + tag `v0.3.0`.

---

## 부속 — Why v0.3.0 (격상 사유)

| 영역 | v0.2.1 frozen | v0.3.0 candidate |
|---|---|---|
| **Director Card** | v1.0 (4약속 only) | v1.1 (4약속 + 조건 A/B/C/D) |
| **Next cycle 선택** | 암묵 (일부 D-017 잔존) | D-025 자율 default 표준 |
| **FIL ledger** | source project meta 만 | Foundry 측 adopted ledger 박제 |
| **Field validation** | Phase 1+2 | Phase 1~5 (WP-002~005 추가) |
| **Operating data** | 8 FIL (FIL-001~008) | 14 FIL 누적 (FIL-001~014), 3 adopted |

**핵심**: v0.2.1 의 정책 layer 위에 *reduced-copy 의 자연 연장* (cycle 선택 자체도 reduced-copy) + *Director Card 강화 4 조건* + *재현 가능한 FIL 의 standard 박제*. 코드 변경 0 → 회귀 risk 0.
